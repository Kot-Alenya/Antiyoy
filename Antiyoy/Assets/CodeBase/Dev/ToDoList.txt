rename RegionType to TeamType/TeamColor (?)
change hotkeys to return next/back

mapEditor: 
    worldExport;

bootstrap & hub
    loadWorld (goto gameplay);
    mapEditor load;
    
gameplay logic:
    players;
    capturing regions;
    win;
    lose;
