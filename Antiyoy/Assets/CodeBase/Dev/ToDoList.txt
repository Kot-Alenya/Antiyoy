bugFix;

remove regionRebuilder interface as useless.
integrate worldVersion in terrain as connected operations;(?)

mapEditor: 
    worldExport;

bootstrap & hub
    loadWorld (goto gameplay);
    mapEditor load;
    
gameplay logic:
    players;
    capturing regions;
    win;
    lose;
