геймплей: загрузка мира => вобор игрока(всегда игрок - крассный) => захват регионов, победа.

leave gameplay/worldEditor buttons.

gameplay logic:
    player;
    grabbing regions;
    win;
    ai (?);
    
refactoring:
    worldEditor ui;
    rename RegionType to PlayerType/PlayerColor (?);
    world save/load key to static!
    split installers (?);
    static data from interfaces to SO!
    is debug window actually need IStaticData config from dataProvider?
    refactoring staticDataProvider. 