rename RegionType to TeamType/TeamColor (?)

control IStaticData bind!

bootstrap & hub
    loadWorld (goto gameplay);
    mapEditor load;
    
gameplay logic:
    players;
    capturing regions;
    win;
    lose;
